// JavaScript code to access and display the data attribute value
const element = document.getElementById('elementWithAttributes');
const dataAttributeValue = element.getAttribute('data-custom-attribute');

// Displaying the data attribute value in the console
console.log('Data Attribute Value:', dataAttributeValue);
